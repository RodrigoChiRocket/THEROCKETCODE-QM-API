<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qualitas.portal.fraudes.account.Infrastructure.dao.PersonaDao">

    <!-- Incluir el TypeHandler para el campo Sexo -->
    <resultMap id="PersonaResultMap" type="com.qualitas.portal.fraudes.account.domain.model.Persona">
        <id property="iPersonaId" column="I_PERSONA_ID"/>
        <result property="vNombre" column="V_NOMBRE"/>
        <result property="vSexo" column="V_SEXO" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="iCodigoPostal" column="I_CODIGO_POSTAL"/>
        <result property="dFechaNacimiento" column="D_FECHA_NACIMIENTO"/>
        <result property="vCorreo" column="V_CORREO"/>
        <result property="vNumeroTelefonico" column="V_NUMERO_TELEFONICO"/>
    </resultMap>

    <!-- Consulta para crear una Persona -->
    <insert id="crearPersona" parameterType="com.qualitas.portal.fraudes.account.domain.model.Persona" useGeneratedKeys="true" keyProperty="iPersonaId">
        INSERT INTO T_PERSONAS_MC (V_NOMBRE, V_SEXO, I_CODIGO_POSTAL, D_FECHA_NACIMIENTO, V_CORREO, V_NUMERO_TELEFONICO)
        VALUES (#{vNombre}, #{vSexo, typeHandler=org.apache.ibatis.type.EnumTypeHandler}, #{iCodigoPostal}, #{dFechaNacimiento}, #{vCorreo}, #{vNumeroTelefonico})
        <selectKey keyProperty="iPersonaId" resultType="BigDecimal" order="AFTER">
            SELECT SEQ_PERSONA_ID_MC.CURRVAL FROM dual
        </selectKey>
    </insert>

    <!-- Consulta para obtener una Persona por ID -->
    <select id="obtenerPersona" parameterType="java.math.BigDecimal" resultMap="PersonaResultMap">
        SELECT I_PERSONA_ID, V_NOMBRE, V_SEXO, I_CODIGO_POSTAL, D_FECHA_NACIMIENTO, V_CORREO, V_NUMERO_TELEFONICO
        FROM T_PERSONAS_MC
        WHERE I_PERSONA_ID = #{id}
    </select>

    <!-- Consulta para actualizar una Persona -->
    <update id="actualizarPersona" parameterType="com.qualitas.portal.fraudes.account.domain.model.Persona">
        UPDATE T_PERSONAS_MC
        SET V_NOMBRE = #{vNombre},
            V_SEXO = #{vSexo, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
            I_CODIGO_POSTAL = #{iCodigoPostal},
            D_FECHA_NACIMIENTO = #{dFechaNacimiento},
            V_CORREO = #{vCorreo},
            V_NUMERO_TELEFONICO = #{vNumeroTelefonico}
        WHERE I_PERSONA_ID = #{iPersonaId}
    </update>

    <!-- Consulta para listar todas las Personas -->
    <select id="listarPersonas" resultMap="PersonaResultMap">
        SELECT I_PERSONA_ID, V_NOMBRE, V_SEXO, I_CODIGO_POSTAL, D_FECHA_NACIMIENTO, V_CORREO, V_NUMERO_TELEFONICO
        FROM T_PERSONAS_MC
    </select>

    <!-- Consulta para eliminar una Persona -->
    <delete id="eliminarPersona" parameterType="java.math.BigDecimal">
        DELETE FROM T_PERSONAS_MC
        WHERE I_PERSONA_ID = #{id}
    </delete>
</mapper>
